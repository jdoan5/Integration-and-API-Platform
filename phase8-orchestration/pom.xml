<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/>
	</parent>
	<groupId>com.jdoan</groupId>
	<artifactId>orchestration</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>orchestration</name>
	<description>Durable replenishment saga on Temporal</description>

	<properties>
		<java.version>21</java.version>
		<temporal.version>1.38.0</temporal.version>
	</properties>

	<dependencies>
		<!-- The plain SDK, with the client and worker wired by hand in
		     TemporalConfig.

		     There IS a working Spring starter - io.temporal:temporal-spring-boot-starter
		     at 1.38.0, which supports Boot 4. (Do not confuse it with
		     temporal-spring-boot-starter-alpha: the -alpha suffix was dropped
		     at 1.24.0 and that artifact has been frozen since April 2024.)

		     Choosing the plain SDK here is a preference, not a necessity: the
		     wiring is about thirty lines, it has no version-skew risk against
		     Boot 4, and it keeps the moving parts visible - a stub, a client,
		     and a worker polling one queue. The starter would be the right call
		     in a project with many workers to auto-discover. -->
		<dependency>
			<groupId>io.temporal</groupId>
			<artifactId>temporal-sdk</artifactId>
			<version>${temporal.version}</version>
		</dependency>
		<dependency>
			<groupId>io.temporal</groupId>
			<artifactId>temporal-opentracing</artifactId>
			<version>${temporal.version}</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-opentelemetry</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>io.temporal</groupId>
			<artifactId>temporal-testing</artifactId>
			<version>${temporal.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
		</plugins>
	</build>
</project>
